# 发布回滚预案

| 项 | 内容 |
|---|---|
| 预案编号 | RL-001 |
| 版本 | v1.0 |
| 适用范围 | 生产环境所有对外发布（应用版本、配置、数据变更、特性开关） |
| 制定/维护人 | 发布负责人（Release Owner） |
| 更新频率 | 每季度评审一次，或每次重大架构变更后更新 |

> 目标：在发布引发事故时，能在 **15 分钟** 内完成决策、**30 分钟** 内完成回滚与数据恢复，将业务影响降到最低。

---

## 一、触发条件

| 序号 | 触发条件 | 判定标准 | 优先级 |
|---|---|---|---|
| 1 | **核心功能不可用** | 首页/登录/下单/支付等核心链路错误率 > 5% 持续 3 分钟，或关键接口 5xx 率 > 2% 持续 5 分钟 | P0 |
| 2 | **数据异常** | 核心数据（订单、支付、库存、账户）写入失败率 > 1%，或监控发现主键冲突、数据不一致、金额对账不平 | P0 |
| 3 | **性能严重劣化** | 核心接口 P95 延迟较发布前基线劣化 > 100%，或 CPU/内存/连接数持续打满导致容量告警 | P1 |
| 4 | **安全与合规问题** | 发布内容被证实存在安全漏洞、敏感信息泄露、违规内容或不合规风险 | P0 |

**判定动作**：任一 P0 条件触发，值班负责人须在 **5 分钟内** 召集发布负责人确认，无需等待完整根因分析即可启动回滚；P1 条件由发布负责人评估是否回滚或热修复。

---

## 二、负责人

| 角色 | 负责人（默认） | 职责 | 联系方式 |
|---|---|---|---|
| 1 | 回滚决策人（发布负责人） | 确认触发条件、拍板是否回滚、宣布回滚启动与结束 | 值班电话 + 即时通讯群 |
| 2 | 回滚执行人（后端/运维负责人） | 执行版本回退、流量切换、数据库变更回退，报告执行结果 | 值班电话 |
| 3 | 数据恢复负责人（DBA） | 执行备份恢复、数据订正、一致性校验 | 值班电话 |
| 4 | 验证与通知负责人（QA/接口人） | 执行冒烟与回归验证、向业务方和干系人同步进展 | 即时通讯群 |

**职责边界**：
- 决策人与执行人**不得为同一人**，避免单人失误导致决策与执行互相掩盖。
- 回滚启动后，由验证与通知负责人每 **15 分钟** 在故障群同步一次进展。
- 当默认负责人不在岗时，由其直管上级或其指定代理接任，接任情况须记录在案。

---

## 三、执行步骤

| 序号 | 步骤 | 动作说明 | 负责角色 | 时限 |
|---|---|---|---|---|
| 1 | 宣布回滚并冻结变更 | 在故障群宣布"回滚启动"，**冻结**一切新的发布、配置变更、数据变更，防止二次污染 | 回滚决策人 | 5 分钟内 |
| 2 | 摘除异常流量 | 通过负载均衡/网关将异常节点或新版本全部摘除，将流量切回上一稳定版本集群；保留现场（日志、线程 dump、请求样本）供后续根因分析 | 回滚执行人 | 10 分钟内 |
| 3 | 回滚应用版本 | 将应用/镜像/静态资源回退到上一个稳定发布版本（Release Tag 回退），并同步回退相关配置与特性开关 | 回滚执行人 | 20 分钟内 |
| 4 | 回滚数据变更（如涉及） | 对本次发布携带的数据库 schema/存储过程/初始化数据变更执行逆向脚本回退；无数据变更则跳过并记录 | 数据恢复负责人 | 与步骤 3 并行 |
| 5 | 恢复流量并观察 | 确认版本与数据就绪后，按 10% → 50% → 100% 逐步放量恢复，观察指标稳定后宣布"回滚完成" | 回滚执行人 + 验证负责人 | 放量观察 ≥ 15 分钟 |

**执行纪律**：
- 每一步完成后必须在故障群按固定格式汇报：`[步骤名] 结果：成功/失败，耗时：X 分钟，备注`。
- 若某一步失败，按"失败处理"分支处理：步骤 2 失败则强制重启旧版本进程；步骤 3 失败则切换备用发布通道重新发布旧版本。
- 回滚全程记录时间线，事后 24 小时内补写《事故与回滚复盘报告》。

---

## 四、数据恢复

| 序号 | 动作 | 说明 | 负责角色 |
|---|---|---|---|
| 1 | 回滚前先备份现场数据 | 在执行任何数据回退前，先对当前数据库做逻辑/物理备份（或至少导出受影响表），确保"当前坏状态"可回溯 | 数据恢复负责人 |
| 2 | 恢复 schema 与基线数据 | 执行发布前准备的**逆向迁移脚本**回退 schema；对发布期间被错误修改/污染的数据，从最近一份**发布前备份**恢复或订正，恢复顺序：先主库后从库 | 数据恢复负责人 |
| 3 | 补偿与重放处理 | 对回滚窗口内产生的业务数据（如新版本产生的脏订单、错配数据），按业务规则执行补偿脚本或人工订正；补偿任务必须幂等，可重复执行 | 数据恢复负责人 + 业务方 |
| 4 | 清理缓存与中间态 | 清空受影响的缓存（Redis/本地缓存）、消息队列积压的无效消息、任务调度中的残留任务，防止旧数据被再次消费写入 | 回滚执行人 |
| 5 | 数据对账 | 恢复完成后，对核心表做**行数比对、金额汇总比对、关键字段抽样比对**（与备份或对端系统核对），确认无缺数、无重复、无金额差异 | 数据恢复负责人 |

**数据恢复原则**：
- 以"不丢失、不重复、可对账"为底线；宁可先恢复可用性，再后台异步补数。
- 所有恢复与订正操作记录 SQL/脚本清单与执行人，供审计与复盘。

---

## 五、验证方式

| 序号 | 验证项 | 验证方式 | 通过标准 | 负责角色 |
|---|---|---|---|---|
| 1 | 核心链路冒烟测试 | 对首页、登录、下单、支付等核心链路执行预置冒烟用例（自动化脚本 + 关键用例人工复核） | 冒烟用例 **100% 通过**，无阻塞缺陷 | 验证负责人 |
| 2 | 监控指标回归 | 核对错误率、5xx 率、P95/P99 延迟、容量（CPU/内存/连接数）等监控恢复到发布前基线水平 | 核心指标连续 **15 分钟** 处于基线阈值内 | 回滚执行人 |
| 3 | 数据一致性校验 | 执行对账脚本：核心表行数、金额合计、抽样明细与发布前基线或对端系统比对 | 差异为 0；存在差异时须查明并修复后方可宣布完成 | 数据恢复负责人 |
| 4 | 业务抽样确认 | 由业务方在放量阶段抽样验证关键业务场景（含发布涉及的新功能回退后行为） | 业务方确认无异常，签字或群里确认 | 验证负责人 + 业务方 |

**验证完成标准**：上述 4 项全部通过后，由回滚决策人宣布"回滚完成"，恢复常规发布流程；若任一项未通过，继续修复并重新验证，不得带病放量。

---

## 附录：回滚决策检查清单

- [ ] 是否已确认触发条件并达 P0/P1 判定标准？
- [ ] 是否已冻结新发布与变更？
- [ ] 是否已备份当前现场数据？
- [ ] 是否已摘除异常流量并保留现场证据？
- [ ] 是否已回退应用版本与配置/特性开关？
- [ ] 是否已执行/跳过数据回退并记录？
- [ ] 是否已逐步放量并完成 4 项验证？
- [ ] 是否已记录时间线并安排复盘？
