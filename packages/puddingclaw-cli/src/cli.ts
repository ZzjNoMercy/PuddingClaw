/**
 * Type-level contract for the shipped Node Worker CLI.
 *
 * The checked-in dist/ runtime is intentionally dependency-free so installing
 * the local npm package does not require TypeScript, Python, or a build step.
 * This source tree is validation-only; tsconfig.json must never emit over dist/.
 */
export { };
