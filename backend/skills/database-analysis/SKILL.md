---
name: database-analysis
description: Analyze configured relational data sources through staged schema, SQL generation, validation and execution.
toolsets:
  - database_analysis
  - semantic_lookup
---

# Database Analysis

For a business question, generate SQL from the original wording, validate it,
then execute it. Inspect schema only when the user needs metadata. Use semantic
lookup whenever a published entity dimension is needed for normalization.
